# rough ideas scratchpad  

psychocomponent theory: different components as weights generate stuff that combined predicts actions and thoughts. components can be evolutionary drives, and different patterns of behaviors gets encoded  into components.

generator weights theory: it seems like llms dont have a stable personality or prefernce, that their choices regarding their believes are majorly context swayed, maybe  because they dont have "generator weights" the way a human has. weights that just constantly output a certain  activations. like a person's core personality and motivation etc.  

modeling mechanism is an interesting concept. given a world state and a "modeling  mechanism" such as different personality trait and life circumstance in people, maybe we can predict their world view. in a ml model perhaps given the data and algorithm and training loss we can predict their world view.  

actuator chains: like washing my face -> looking cuter  -> can get a gf -> greencard -> anthropic. basically in an action sequence the latter world states depend on the earlier ones, and maybe considerations of the world state are centered on the actuator.    

generator weights are really just. like the data a human encounters whether enviromental or subsequently from other neurons, is contrained within certain regions. and weights transform that space into other regions. you can see the weights as generator weights that keep outputting those regions. or you can see it as  a rigid data rigid transformation pairing.   

a feature basis is statistical regularities of activation the model uses in real data. features are activation patterns the model actually uses. that is definitionally what makes  them features. it is only by the efficient mind convergence hypothesis that  they happened  to be interpretable. and due to local linearity, we decompose an activation into multiple features rather than considering that  entire activation one feature. SAE decomposes a single activation and WCC and CLT decompose activations cross layer. like biologically we can find neuron firing patterns that are cross different regions.   
and from this what features the model uses becomes an exact question. 
